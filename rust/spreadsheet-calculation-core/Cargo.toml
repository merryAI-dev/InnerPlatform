[package]
name = "spreadsheet-calculation-core"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["alloc", "std"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
